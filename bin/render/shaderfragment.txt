#version 330 core
out vec4 FragColor;
  
in vec3 ourColor;
in vec2 TexCoord;

uniform sampler2D atexture;
uniform vec3 aColor;

void main()
{
    FragColor = texture(atexture, TexCoord) * vec4(aColor, 1.0);
}